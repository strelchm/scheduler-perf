# scheduler-perf
Comparing persistent scheduling libs with load testing

Modes
===================
For db-scheduler enabling db-scheduler profile activating is needed, for example with ENV-s:
`spring.profiles.active=db-scheduler`
JobRunr activating:
`spring.profiles.active=jobrunr`

